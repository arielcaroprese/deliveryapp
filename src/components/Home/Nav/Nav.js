import "./nav.css";

const Nav = () => {
    return (
        <nav>
           <h1 className="navLogo">Logo Img</h1>
           <button className="navLoginButton">Iniciar Sesión</button>
        </nav>
    );
}

export default Nav;